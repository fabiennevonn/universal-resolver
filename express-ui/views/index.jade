h1 DID Resolver (Universal Resolver)

form(method="POST" action="/resolve")
  input(
    type="text"
    name="did"
    placeholder="did:key:z6Mk..."
    style="width: 400px"
  )
  button(type="submit") Resolve

if error
  p(style="color:red")= error

if result
  h2 Ergebnis
  pre= result
